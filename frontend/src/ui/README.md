# UI kit

Reusable visual components for Vite + React. Import from `frontend/src/ui`.

See [docs/ui.md](../../../docs/ui.md).

Rules:

* No hardcoded API URLs
* No Odoo or provider SDKs
* No hackathon business logic
* Local state in the component; Context only for theme, toasts, and the API client
