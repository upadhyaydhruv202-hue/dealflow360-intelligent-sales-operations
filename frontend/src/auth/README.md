# Auth session

Global session only. Visual login lives in `frontend/src/ui/auth/LoginForm.tsx`. HTTP lives in `frontend/src/services/auth.ts`.

Do not put passwords or tokens in logs. Do not call Odoo from this module.
