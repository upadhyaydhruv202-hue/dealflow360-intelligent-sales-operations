# PDF

Low-level PDF generation (title/sections) stored through `StorageService`. Job name: `pdf.generate`.

Report orchestration (templates, tables, jobs, notifications) lives in `integrations/reports`. See `docs/pdf.md` and `docs/reports.md`.
