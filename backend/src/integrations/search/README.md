# Search

Provider-agnostic keyword and full-text search. Controllers and application services call `SearchService`. They never import Elasticsearch or write `to_tsvector` SQL.

See `docs/search.md`.
