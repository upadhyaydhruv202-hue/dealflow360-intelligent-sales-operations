# Analytics

Provider-agnostic KPI aggregations, time-series, dashboards, filters, and exports. Controllers and application services call `AnalyticsService`. They never query a warehouse client or run unbounded `GROUP BY` SQL.

See `docs/analytics.md`.
