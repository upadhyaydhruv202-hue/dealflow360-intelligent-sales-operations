# Reports

Reusable report generation. Templates stay generic; hackathon-specific types belong under `modules/problem`.

Pipeline: request → data provider → template → PDF renderer → StorageService → optional notification/email.

See `docs/reports.md`.
