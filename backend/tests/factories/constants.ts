/** Placeholder hash for persisted users that are not used for password login. */
export const TEST_PASSWORD_HASH = 'test-hash-placeholder-not-a-real-secret';

/** Password that satisfies the default test policy (`AUTH_PASSWORD_MIN_LENGTH=8`). */
export const TEST_PASSWORD = 'correct-horse';

export const FACTORY_CHECKSUM_SHA256 = 'a'.repeat(64);
